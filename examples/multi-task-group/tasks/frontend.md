# Frontend subtask

Minimal SPA (vanilla JS or HTMX) that calls the backend from subtask
`backend`. Pages:

- List users
- Create form
- Edit/delete actions

Acceptance: `npm run build` (or plain static files) produces `./web/dist/`.
